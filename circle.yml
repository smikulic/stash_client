machine:
  node:
    version: 8.1.4
deployment:
  release:
    tag: /release-.*/
    commands:
      - yarn build:production
      - echo 'deploying to production'
      - ssh -v $DO_USER@$DO_IP "cd ~/var/www/scroogevault.co/testing/; ./scripts/deploy_production.sh"
