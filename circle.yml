machine:
  node:
    version: 8.1.4
deployment:
  release:
    tag: /release-.*/
    commands:
      - yarn build:production
      - echo 'deploying to production'
      - scp -r stash_client/dist/* $DO_USER@$DO_IP:/var/www/scroogevault.co/
